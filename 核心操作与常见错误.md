# 核心操作与常见错误速查表

## 🚀 三步启动（最快路径）

### 步骤1：启动工作站

```bash
cd D:\multisim\MediaCrawler-main
streamlit run src/app/dashboard.py
```

### 步骤2：选择数据源

- **演示模式**（推荐）：选择 "Sample Data (演示模式)"
- **真实数据模式**：选择 "Crawled Data (真实数据)"

### 步骤3：点击 Mine

- 点击 **🔍 Mine（挖掘）** 按钮
- 等待 10-30 秒
- 查看图谱+榜单+样本

---

## 📋 核心功能清单

### ✅ 已实现的关键功能

| 功能 | 位置 | 说明 |
|------|------|------|
| **数据源切换** | 侧边栏 | Crawled / Sample 数据 |
| **一键挖掘** | 侧边栏 | Mine 按钮（带进度+日志）|
| **样例库管理** | 侧边栏 | 覆盖/合并去重到 raw |
| **数据统计** | 侧边栏 | Raw/Clean 行数+路径 |
| **导出提交包** | 侧边栏 | 生成 submission_<time>.zip |
| **图谱可视化** | Tab1 | 交互式 HTML（可下载）|
| **PageRank Top** | Tab1 | 核心标签排名表格 |
| **Rising Edges** | Tab1 | 趋势组合榜单（带窗口样本数）|
| **原帖样本** | Tab2 | 笔记卡片展示（分页）|
| **详细日志** | 底部 | 实时日志（最近10条）|

---

## ⚠️ 常见错误及解决方案

### ❌ 错误1：启动失败 - `ModuleNotFoundError`

**现象**：
```
ModuleNotFoundError: No module named 'streamlit'
```

**解决**：
```bash
uv pip install streamlit pandas networkx pyvis
```

---

### ❌ 错误2：数据文件不存在

**现象**：Dashboard 显示 "数据文件为空"

**解决**：
```bash
# 方法1：使用 Sample Data 模式（最简单）
# 在 Dashboard 左侧选择 "Sample Data (演示模式)"

# 方法2：重新爬取数据
uv run python scripts/test_crawl_raw.py
python scripts/clean_data_direct.py
```

---

### ❌ 错误3：图谱为空 / PageRank 为空

**现象**：Mine 后提示"标签数量不足"

**原因**：数据太少（<5条）或标签太少

**解决**：
1. **立即解决**：切换到 "Sample Data (演示模式)"
2. **根本解决**：继续爬取数据至30+条

---

### ❌ 错误4：Rising Edges 为空

**现象**：趋势榜显示 "Recent 窗口数据不足"

**说明**：**这不是错误！** 是正常现象

**原因**：
- 20条数据时间跨度可能<7天
- 所有数据都在 Recent 窗口内，无历史对比

**展示说明**：
- Dashboard 会显示窗口样本数（Recent: X, Historical: Y）
- 这体现了"科学严谨"（不造假数据）

---

### ❌ 错误5：导出提交包失败

**现象**：提示"打包失败"

**原因**：某些文件不存在（如 graph.html）

**解决**：
```bash
# 确保先运行 Mine，生成所有文件
# 然后再点击导出按钮
```

---

### ❌ 错误6：端口 8501 占用

**现象**：
```
Port 8501 is in use by another program
```

**解决**：
```bash
# 使用其他端口
streamlit run src/app/dashboard.py --server.port 8502

# 或关闭占用的进程
taskkill /F /IM streamlit.exe
```

---

## 🎯 演示流程（给老师/导师展示）

### 准备阶段（演示前1天）

```bash
# 1. 确保依赖安装
uv sync

# 2. 测试启动
streamlit run src/app/dashboard.py

# 3. 使用 Sample Data 模式测试一遍完整流程
```

### 演示当天

**强烈推荐**：使用 **Sample Data (演示模式)**

**优势**：
- ✅ 稳定可靠（不依赖网络/登录）
- ✅ 数据充足（图谱和榜单都有内容）
- ✅ 响应快速（<10秒）

**流程**：
1. **启动**：`streamlit run src/app/dashboard.py`
2. **介绍**："这是一个端到端的社交媒体数据挖掘系统"
3. **演示数据源切换**："支持真实数据和演示数据"
4. **点击 Mine**：展示进度条和日志（体现可观测性）
5. **解读图谱**：
   - "节点大小代表 PageRank 重要性"
   - "边粗细代表共现频率"
   - "颜色代表社区"
6. **解读 PageRank Top**：
   - "这是基于图结构的核心话题标签"
   - "体现了用户讨论的中心议题"
7. **解读 Rising Edges**：
   - "这是时间窗口对比发现的趋势组合"
   - "Recent X条 vs Historical Y条"
   - 如果为空："由于数据时间跨度限制，这是科学严谨的结果"
8. **展示原帖样本**："数据来源清晰可追溯"
9. **导出提交包**：
   - 点击 "生成 Submission ZIP"
   - 下载
   - 说明："这个包包含所有数据、报告、文档，符合工程规范"

---

## 💡 答辩话术要点

### 1. 强调"工程规范"

> "我们实现了**完整的数据管道**：从爬取、清洗、挖掘到可视化，每个阶段都有日志和报告，确保**可复现性**。"

### 2. 强调"科研价值"

> "使用**图结构挖掘**方法，基于 PageRank 发现核心话题，基于时间窗口对比发现趋势组合，体现了**数据科学的研究深度**。"

### 3. 强调"稳定性"

> "实现了 **Demo Mode 样例库**，即使网络或登录失败，演示也能正常进行，这是生产环境的基本要求。"

### 4. 应对 Rising Edges 为空

> "Rising Edges 显示 Recent 样本为0，这是因为我们采用了**科学严谨的时间窗口划分**，不会为了展示效果而造假数据。这恰恰体现了系统的可信度。"

---

## 🔧 常用操作命令

### 爬取数据

```bash
# 抓取 AI工具 相关（5条）
uv run python scripts/test_crawl_raw.py

# 抓取更多（修改 scripts/test_crawl_raw.py 第22行 max_notes=40）
uv run python scripts/test_crawl_raw.py
```

### 清洗数据

```bash
python scripts/clean_data_direct.py
```

### 启动工作站

```bash
streamlit run src/app/dashboard.py
```

### 查看数据

```bash
# 查看原始数据
type data\raw\annotations.jsonl | more

# 查看清洗后数据  
type data\clean\annotations_clean.jsonl | more

# 查看清洗报告
type data\stats\cleaning_report.json

# 统计行数
find /c /v "" data\clean\annotations_clean.jsonl
```

### 导出提交包

在 Dashboard 左侧点击 **🎁 生成 Submission ZIP**

---

## 📊 数据文件说明

| 文件路径 | 说明 | 何时生成 |
|---------|------|----------|
| `data/raw/annotations.jsonl` | 原始数据 | 爬虫运行时 |
| `data/clean/annotations_clean.jsonl` | 清洗后数据 | 清洗脚本 |
| `data/samples/annotations_sample.jsonl` | 演示样例 | 自动复制 |
| `data/stats/cleaning_report.json` | 清洗报告 | 清洗脚本 |
| `data/output/graph.html` | 交互式图谱 | Mine 时生成 |
| `data/exports/submission_*.zip` | 提交包 | 导出时生成 |
| `logs/app.log` | 系统日志 | 运行时生成 |

---

## ✅ 完整验收清单

### 启动验收

- [ ] `streamlit run src/app/dashboard.py` 能正常启动
- [ ] 浏览器自动打开 `http://localhost:8501`
- [ ] 页面无报错，所有控件正常显示

### 功能验收

- [ ] 数据源可以切换（Crawled ↔ Sample）
- [ ] Mine 按钮点击后有进度条和日志
- [ ] 图谱能正常显示（可拖拽、缩放）
- [ ] PageRank Top 榜单非空（至少5个）
- [ ] Rising Edges 显示窗口样本数
- [ ] 原帖样本能分页展示
- [ ] 样例数据操作（覆盖/合并）正常
- [ ] 导出提交包能生成并下载

### 稳定性验收

- [ ] 使用 Sample Data 模式永不报错
- [ ] 日志清晰显示每步成功/失败
- [ ] 异常有友好提示（不是技术错误信息）

---

## 🎉 成功标志

看到以下内容说明系统完全就绪：

```
✅ 爬虫能稳定运行（20+ 条数据）
✅ Dashboard 能正常启动
✅ Sample Data 模式能出图+榜单
✅ 导出提交包功能正常
✅ 日志实时显示，无"转圈"
✅ 所有异常都有明确提示
```

---

**准备演示？直接运行：`streamlit run src/app/dashboard.py`** 🚀
